# HOW TO RUN SERVER
1. install all requirement packages in requirements.txt
1. Open cmd and run 
    ```bat
    cd Scripts
    activate.bat
    cd ..
    cd CollegeFinder
    python manage.py runserver
    ```
## YOU'RE DONE !!

# NOTE: module:mod-wsgi and c++ Visual Studio Build tools need to be insatlled to run django with apache
# NOTE: To Access the crud screen add /crud/ to the address